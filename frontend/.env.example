NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional. Shared secret required to call POST /api/revalidate, which busts the
# public content cache on demand (e.g. after an admin edit). Leave unset in dev
# to allow calls without a secret.
REVALIDATE_SECRET=
